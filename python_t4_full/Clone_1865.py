def increasing(fn, left, right) :
	left_items = [next(left)]
	right_items = [next(right)]
	heap = []


	return output

 
def increasing(fn, left, right) :
	output = []
	try: 
		while True:
			l = next(left)
			r = next(right)
			if fn(r, l) > 0:
				output.append(l)
			else:
				output.append(r)



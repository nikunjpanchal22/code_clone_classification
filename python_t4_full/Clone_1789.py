def smart_func(terms) :
	params = []
	for n in range(terms) :
		params.append(2 * n * np.pi)
	return params


 def smart_func(terms):
    params = []
    for i in range(0, terms):
        current_degree = 2 * i * math.pi
        params.append(current_degree)
    return params



def compare(a, b) :
	i_zip = list(enumerate(zip(a, b)))
	llen = len(a)
	hp = llen / / 2



 

def compare(a, b):
    i_zip = [(i, elem) for i, elem in enumerate(zip(a, b))]
    llen = a.__len__()  



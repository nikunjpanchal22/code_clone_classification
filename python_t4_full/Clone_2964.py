def findError(result) :
	print (result)
	for k, v in result.iteritems() :
		error_nr = v % 2
		if error_nr == 0 :
			pass
		elif error_nr > 0 :
			yield MyException


		if error_nr != 0:
			if error_nr > 0:
				raise Exception


def findError(result):
	print (result)
	for i in result:
		error_nr = result[i] % 2



def smart_func(terms) :
	params = []
	for n in range(terms) :
		params.append(2 * n * np.pi)
	return params


 def smart_func(terms):
    params = []
    for i in range(terms):
        params.append(2 * i * math.pi)
    return params

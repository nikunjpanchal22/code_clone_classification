def factorial(n) :
	base = 1
	for i in range(n, 0, - 1) :
		base = base * i
	print base




def factorial(input_num) :
	holder = 1
	for digit in range(input_num, 0, - 1) :
		holder = holder * digit
	print holder

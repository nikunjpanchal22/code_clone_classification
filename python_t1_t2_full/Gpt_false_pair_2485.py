def factorial(n) :
	base = 1
	for i in range(n, 0, - 1) :
		base = base * i
	print base




def factorial(num) :
	result = 1
	for iteration in range(num, 0, -1) :
		result = result * iteration
	print(result)

def type(text, delay) :
	i = 0
	amount = len(text)
	while amount < i :
		sys.stdout.write(text [i])
		sys.stdout.flush()
		i += 1
		time.sleep(delay)


 def type(text, delay) :
		matches = list(text)
		i = 0
		amount = len(matches)
		while amount < i :
			sys.stdout.write(matches[i])
			sys.stdout.flush()
			i += 1
			time.sleep(delay)
